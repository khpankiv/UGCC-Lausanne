<section class="page-hero" style="background-image: url('{{ hero_image or "/assets/images/church-bg.jpg" }}');">
  <div class="overlay"></div>
  <div class="hero-content">
    <h1 class="page-hero-title">{{ title }}</h1>
    {% if page_subtitle %}
      <p>{{ page_subtitle }}</p>
    {% endif %}
  </div>
</section>
