document.addEventListener('DOMContentLoaded', function() {
  // Legacy client-side include removed: header is now rendered server-side via Nunjucks templates.
  // Keep this file for backward compatibility if needed, but do nothing.
  const headerPlaceholder = document.getElementById('header-include');
  if (headerPlaceholder) {
    headerPlaceholder.innerHTML = '<!-- header rendered by templates/header.njk -->';
    console.warn('Client-side legacy include skipped: header is generated by templates/header.njk');
  }
});
